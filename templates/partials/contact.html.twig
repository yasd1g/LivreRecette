{% extends "base.html.twig" %}

{% block  title %}Contactez moi {% endblock %}

{% block body %}

    <div class="container">

        <h1 class="my-3 text-center">Me contacter !</h1>
        <hr style="width: 50%">

        {{ form_start(form) }}

            <div>
                <div class="row d-flex justify-content-center">
                    <div class="col-md-3">
                        {{ form_row(form.firstName) }}
                    </div>
                    <div class="col-md-3">
                        {{ form_row(form.lastName) }}
                    </div>
                </div>
                <div class="row d-flex justify-content-center">
                    <div class="col-md-3">
                        {{ form_row(form.phone) }}
                    </div>
                    <div class="col-md-3">
                        {{ form_row(form.email) }}
                    </div>
                </div>
                <div class="row d-flex justify-content-center ">
                    <div class="col-md-3">
                    </div>
                    <div class="col-md-6">
                        {{ form_row(form.message) }}
                    </div>
                    <div class="col-md-3">
                    </div>
                </div>
                <button class="btn btn-info float-right">
                <i class="far fa-paper-plane"></i>
                Envoyer
                </button>
            </div>





        {#<div class="row">#}
            {#<div class="col-md-3"></div>#}
            {#<div class="col-md-6 alert alert-light">#}
                {#{{ form_widget(form) }}#}

                {#<button class="btn btn-info float-right">#}
                    {#<i class="far fa-paper-plane"></i>#}
                    {#Envoyer#}
                {#</button>#}
            {#</div>#}
            {#<div class="col-md-3"></div>#}
        {#</div>#}
        {{ form_end(form) }}
    </div>

{% endblock %}

