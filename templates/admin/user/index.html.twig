{% extends 'admin/base.html.twig' %}

{% block title %}Administration des utilisateurs !{% endblock %}

{% block body %}
    <div class="container-fluid">
        <h1 class="my-5">Adminitration des utilisateurs</h1>

        <table class="table table-hover">
            <thead class="thead-light">
            <tr>
                <th>Id</th>
                <th>Prénom</th>
                <th>Nom</th>
                <th>Email</th>
                <th>Avatar</th>
                <th>Introduction</th>
                <th>Description</th>
                <th>Action</th>
            </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.id }}</td>
                    <td>{{ user.firstName }}</td>
                    <td>{{ user.lastName }}</td>
                    <td>{{ user.email }}</td>
                    <td><img src="{{ user.picture }}" alt="Avatar de {{ user.fullName }}" class="avatar avatar-medium mr-2"></td>
                    <td>{{ user.introduction | raw }}</td>
                    <td>{{ user.description | raw }}</td>
                    <td>
                        <a href="#" class="btn btn-info"><i class="fas fa-edit"></i></a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>


        <div class="navigation d-flex justify-content-center my-3">
            {{ knp_pagination_render(users) }}
        </div>
    </div>
{% endblock %}
